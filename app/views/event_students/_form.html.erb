<%= form_for(event_student) do |f| %>
  <% if event_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_student.errors.count, "error") %> prohibited this event_student from being saved:</h2>

      <ul>
      <% event_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %>
    <%= f.text_field :student_id %>
  </div>

  <div class="field">
    <%= f.label :event_id %>
    <%= f.text_field :event_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
